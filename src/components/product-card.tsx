import Link from "next/link";import { formatMoney } from "@/lib/money";
export function ProductCard({p}:{p:{slug:string;name:string;price:unknown;oldPrice:unknown;stock:number;category:{name:string}}}){return <article className="card flex flex-col gap-3"><div className="h-32 rounded bg-slate-100 text-center pt-12 text-slate-400">СтройДом</div><p className="text-xs text-slate-500">{p.category.name}</p><Link href={`/products/${p.slug}`} className="font-bold">{p.name}</Link><div className="mt-auto"><b className="text-lg">{formatMoney(String(p.price))}</b>{p.oldPrice&&<del className="ml-2 text-sm text-slate-400">{formatMoney(String(p.oldPrice))}</del>}<p className="text-sm text-green-700">{p.stock>0?"В наличии":"Нет в наличии"}</p></div></article>}
