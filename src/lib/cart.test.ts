import { describe,it,expect } from "vitest";import { cartTotal,changeQuantity } from "./cart";const lines=[{productId:"a",name:"A",price:100,stock:3,quantity:1}];describe("cart",()=>{it("calculates sum",()=>expect(cartTotal(lines)).toBe(100));it("changes quantity",()=>expect(changeQuantity(lines,"a",2)[0].quantity).toBe(2));it("rejects stock overflow",()=>expect(()=>changeQuantity(lines,"a",4)).toThrow())});
