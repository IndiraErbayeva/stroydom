import Link from "next/link";import { prisma } from "@/lib/prisma";import { Header } from "@/components/header";import { ProductCard } from "@/components/product-card";
export default async function Home(){const [products,categories]=await Promise.all([prisma.product.findMany({where:{active:true},include:{category:true},take:8,orderBy:{createdAt:"desc"}}),prisma.category.findMany({where:{active:true}})]);return <><Header/><main><section className="bg-navy py-20 text-white"><div className="container"><p className="text-orange font-bold">СТРОИТЕЛЬСТВО НАДЁЖНОСТИ</p><h1 className="mt-3 max-w-xl text-4xl font-black">Материалы для вашего дома — в одном месте</h1><p className="mt-4 max-w-lg text-slate-300">Доставка по Алматы и Казахстану. Проверенное качество, честные цены.</p><Link href="/catalog" className="btn mt-7">Перейти в каталог</Link></div></section><section className="container py-12"><h2 className="text-2xl font-black">Категории</h2><div className="mt-5 grid grid-cols-2 gap-3 md:grid-cols-4">{categories.map(c=><Link className="card font-bold hover:border-orange" href={`/catalog?category=${c.slug}`} key={c.id}>{c.name}</Link>)}</div></section><section className="container pb-14"><h2 className="text-2xl font-black">Популярные товары</h2><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{products.map(p=><ProductCard key={p.id} p={p}/>)}</div></section></main><footer className="bg-navy py-8 text-white"><div className="container">СтройДом · Алматы · +7 (700) 000-00-00</div></footer></>}
